/**
 * 
 */

function examine(){
	console.log("here")
	let s = document.getElementById("julia");
	s.innerHTML = "valid";
}

function myFunction(p1, p2) {
  return p1 * p2;   // The function returns the product of p1 and p2
}